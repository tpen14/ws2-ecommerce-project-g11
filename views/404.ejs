<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .error-page {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 2rem;
            text-align: center;
        }
        .error-container {
            max-width: 700px;
            background: linear-gradient(135deg, rgba(30, 35, 40, 0.95) 0%, rgba(10, 20, 40, 0.95) 100%);
            padding: 3rem 2rem;
            border-radius: 12px;
            box-shadow: 0 8px 40px rgba(0, 0, 0, 0.9);
            border: 2px solid var(--lol-gold);
        }
        .error-icon {
            font-size: 6rem;
            margin-bottom: 1.5rem;
            filter: drop-shadow(0 0 20px rgba(200, 155, 60, 0.5));
        }
        .error-code {
            font-size: 5rem;
            font-weight: 900;
            color: var(--lol-gold);
            text-shadow: 0 0 30px rgba(200, 155, 60, 0.8);
            margin: 0;
            letter-spacing: 4px;
        }
        .error-title {
            font-size: 2rem;
            color: var(--lol-white);
            margin: 1rem 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .error-message {
            color: var(--lol-light-gray);
            font-size: 1.1rem;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .error-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 1.5rem;
        }
        .error-search {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(200, 155, 60, 0.3);
        }
        .search-form {
            display: flex;
            gap: 0.75rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        .search-form input {
            min-width: 250px;
            flex: 1;
            max-width: 400px;
        }
    </style>
</head>
<body class="error-page">
    <div class="error-container">
        <div class="error-icon">🧭</div>
        <h1 class="error-code">404</h1>
        <h2 class="error-title">Page Not Found</h2>
        <p class="error-message">
            The path you seek does not exist. The link may be broken or the page may have been moved to another dimension.
        </p>
        
        <div class="error-actions">
            <a href="/" class="primary-button">Return Home</a>
            <% if (locals.user) { %>
                <a href="/users/dashboard" class="secondary-button">Dashboard</a>
            <% } else { %>
                <a href="/users/login" class="secondary-button">Login</a>
            <% } %>
        </div>

        <div class="error-search">
            <form class="search-form" action="/products" method="GET">
                <input type="text" name="search" placeholder="Search products..." aria-label="Search">
                <button type="submit" class="primary-button">Search</button>
            </form>
        </div>
    </div>
</body>
</html>